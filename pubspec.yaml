name: aplikasitest1
description: Tugas UI/UX Flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.22.0"

publish_to: 'none'

version: 1.0.0+1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true